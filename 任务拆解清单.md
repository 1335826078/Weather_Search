# 天气查询应用 · 任务拆解清单

> 用途：按下面的顺序，**一次只把一条任务发给 AI**。每条任务都有验收动作，验收不通过就不要进入下一条。

---

## 一、怎么用这份清单

### 对话模板
```
按「任务 T0.1」执行。只改指定的文件，不要顺手动其他文件。
完成后告诉我：改了哪些文件、怎么验证、`npm run typecheck` 结果。
```

### 三条铁律
1. **一次一条** —— 收到上一条的验收结果后再发下一条
2. **不许越界** —— 每条都写了「⛔ 本步不做」，AI 顺手改别处必打断
3. **先跑类型检查** —— 每条结束必须 `npm run typecheck` 通过；不通过就让 AI 只修类型错误，不加新功能

### AI 跑偏的信号（出现就喊停）
- 一次性改了 5 个以上文件
- 说要「顺便重构一下」/「顺手优化」
- 新增了契约里没有的类型或字段
- 在组件里直接写 `fetch`
- 用 `any` 或 `as` 绕过类型错误
- 说「无法验证」但没让你自己跑命令

### 阶段划分
| 阶段 | 目标 | 任务 |
|------|------|------|
| S0 | 修掉现有代码的 4 个真 bug，让 MVP 真的能用 | T0.1 ~ T0.6 |
| S1 | 用真实响应固化数据层，加单元测试 | T1.1 ~ T1.2 |
| S2 | 把展示层做扎实 | T2.1 ~ T2.4 |
| S3 | 收藏城市功能 | T3.1 ~ T3.3 |
| S4 | 状态体验 + 组装 + 回归 | T4.1 ~ T4.4 |
| S5 | （可选）7 天预报 | T5.1 |

> 当前代码已完成一轮实现、`tsc --noEmit` 通过，但存在 4 个会导致功能失效的 bug（已定位到行），所以必须从 S0 开始。

---

# S0 · 基线修复

## T0.1 修复城市搜索参数二次编码
**目标**：让中文城市名能正常被接口识别。

**文件**：`src/services/weather.ts`

**要求**
- `URLSearchParams` 在 `toString()` 时已自动编码，**删除** 手动的 `encodeURIComponent(city)`
- 正确写法：`params.set('city', city)`
- 同时删掉 `buildUrl` 末尾那个两个分支返回相同结果的无用三元表达式

**验收**
1. 页面搜"青岛"，Network 面板里 `city` 参数应为 `%E9%9D%92%E5%B2%9B`（不是 `%25E9...`）
2. 返回的 `city` 字段是「青岛」，不是 IP 所在城市

**⛔ 本步不做**：不改错误处理、不加新参数、不碰 `hooks/`

---

## T0.2 修复天气图标映射
**目标**：让天气图标能正确匹配。

**文件**：`src/utils/weather.ts`

**要求**
- `wea_img` 返回的是**拼音字符串**，改 key 为：
  `qing / yun / yin / yu / lei / xue / wu / shachen / bingbao`
- 删除 `{'0':'qing', '1':'yun', ...}` 这套数字 key（线上永远命中不了）
- 保留中文 key 作为兜底，最终兜底仍为 `'yun'`
- 抽成模块级常量（不要每次调用重建对象）

**验收**
1. 单测或临时打印：`normalizeWeatherIcon('qing') === 'qing'`、`normalizeWeatherIcon('yu') === 'yu'`
2. 页面搜一个晴天城市，图标不再是"云"

**⛔ 本步不做**：不改其他归一化函数

---

## T0.3 修复请求取消与过期响应
**目标**：快速切换城市时，不被旧请求的结果覆盖。

**文件**：`src/services/weather.ts`、`src/hooks/useWeather.ts`

**问题现状**：`useWeather` 里建了 `controllerRef`，但 `fetchWeather` 内部又建了自己的 `AbortController`，两者没打通 —— 取消是空操作；且没有比对机制，先发后到的响应会覆盖新数据。

**要求**
- `fetchWeather(url, signal?: AbortSignal)` 接收外部信号，不再内部自建
- `useWeather` 维护一个自增 `requestIdRef`，每次请求 `++`；响应回来后比对 `myId !== requestIdRef.current` 则**直接丢弃，不 setState**
- 组件卸载时 abort 在途请求
- `AbortError` 不要显示成错误提示（属于正常取消）

**验收**
1. 连续快速点 3 个热门城市，最终页面展示的是最后一次点击的城市
2. 切页面/卸载组件时控制台无 "setState on unmounted component" 警告

**⛔ 本步不做**：不引入状态管理库、不改缓存策略

---

## T0.4 修复重试按钮
**目标**：请求失败后，重试按钮真的能重试。

**文件**：`src/hooks/useWeather.ts`

**问题现状**：`refresh()` 依赖 `cacheRef` 里有缓存才执行，而请求失败时不会写缓存 → 重试是空操作。

**要求**
- 用 ref 记录「最近一次请求的城市参数」（`lastParamsRef`），每次 `loadWeather` 成功或失败都更新
- `refresh()`：优先用 `lastParamsRef` 重试；为空时退回默认城市「北京」
- 不依赖缓存是否存在

**验收**
1. 开发者工具切 Offline → 搜索 → 出现错误 → 恢复正常网络 → 点重试 → 成功加载
2. 首次进入就失败时，点重试也能正常发起请求

**⛔ 本步不做**：不改错误文案、不加自动重试

---

## T0.5 冻结温度缺失值契约
**目标**：缺失温度显示 `--`，而不是 `0°` 或 `NaN`。

**文件**：`src/types/weather.ts`、`src/utils/format.ts`、`src/utils/weather.ts`

**要求**
- `temperature` / `high` / `low` 类型改为 `number | null`（`HourlyForecast.temperature` 同理）
- `format.ts` 新增 `toNumber(v: string): number | null`：`Number.isFinite(Number(v))` 通过才返回取整值，否则 `null`
- 新增 `formatTemp(v: number | null): string`：`null → '--'`，否则 `'25°'`
- 归一化里把 `formatTemperature(raw.tem) || 0` 这类写法全部替换为 `toNumber(raw.tem)`
- 删除旧的 `formatTemperature` 用法（避免两套并存）

**验收**
1. `npm run typecheck` 会报出所有需要适配的组件 —— 记录下来，留给 T0.6
2. 单测：`toNumber('abc') === null`、`toNumber('') === null`、`toNumber('25.6') === 26`

**⛔ 本步不做**：本步只改类型和工具函数，**不要动组件**（组件留给 T0.6）

---

## T0.6 UI 适配温度契约
**目标**：让展示组件适配 `number | null`。

**文件**：`src/components/CurrentWeather.tsx`、`HourlyForecast.tsx`、`DailyForecast.tsx`、`FavoriteCities.tsx`（以 T0.5 类型检查报错清单为准）

**要求**
- 温度展示统一走 `formatTemp()`
- 不要用 `?? 0` 兜底（会把缺失显示成 0°）
- 不改组件结构与样式

**验收**
1. `npm run typecheck` 0 错误
2. 页面温度显示正常，无 `NaN` / `undefined` / `null` 字样

**⛔ 本步不做**：不重构组件、不调样式

---

# S1 · 数据层验证

## T1.1 抓真实响应做 fixture
**目标**：用真实数据结构固化类型，杜绝"照文档猜字段"。

**文件**：新增 `src/services/__fixtures__/weather-v63.json`、`forecast-v1.json`

**要求**
- 先在浏览器 Network 面板分别抓一次成功响应的完整 JSON，原样保存为 fixture
- 对照 fixture 核对 `types/weather.ts` 的 `WeatherApiRaw` 是否每个字段都对得上
- 有出入就改类型（**以真实响应为准，不以文档为准**）
- 记录一次"无预警"和一次"有预警"的样例

**验收**
1. fixture 文件存在且是完整 JSON
2. 类型字段与 fixture 一一对应，无多余无缺失

**⛔ 本步不做**：不写业务逻辑

---

## T1.2 归一化函数单元测试
**目标**：把"bug 重灾区"用测试锁住。

**文件**：新增 `src/utils/weather.test.ts`

**要求**：用 T1.1 的 fixture 覆盖以下用例
- 图标映射：9 种 `wea_img` 全部命中，未知值兜底 `'yun'`
- 温度：`'25.6' → 26`、`''→ null`、`'abc' → null`
- `alarm` 为**对象**时能转成单元素数组
- `alarm` 为**数组**时正常映射
- `alarm` 缺失时返回空数组
- `hours` 缺失时返回空数组
- 湿度去掉 `%`
- AQI 等级 → `category` 边界（35/75/115/150/250）

**验收**：`npm run test` 全部通过

**⛔ 本步不做**：不做组件测试（MVP 阶段性价比低）

---

# S2 · 展示层

## T2.1 当前天气卡 + 指标网格
**文件**：`CurrentWeather.tsx`、`WeatherMetrics.tsx`

**要求**
- 城市名 + 更新时间；天气图标 + 文字；实时温度大字（视觉焦点）
- 今日最高 / 最低温
- 指标网格：湿度 / 能见度 / 气压 / 风向风力 / 风速
- 移动端单列、桌面端网格；点击区域 ≥ 44px
- 类名全部为完整静态类名

**验收**：PC 与手机宽度下布局都不溢出、不重叠

**⛔ 本步不做**：不加图表、不做骨架屏

---

## T2.2 空气质量卡配色
**文件**：`AirQualityCard.tsx`、`src/constants/weather.ts`

**要求**
- `AQI_CATEGORY_MAP` 用**完整静态类名**（如 `'bg-emerald-50 text-emerald-700'`）
- 展示 AQI 数值 + 等级 + 提示文案
- 六级配色：excellent 绿 / good 黄 / light 橙 / moderate 红 / heavy 深红 / severe 紫

**验收**：手动改 AQI 数值测试 6 个等级颜色都能切换

**⛔ 本步不做**：不改 AQI 等级判定逻辑（已在 T1.2 测过）

---

## T2.3 24 小时横向滚动
**文件**：`HourlyForecast.tsx`

**要求**
- `overflow-x-auto` + 隐藏滚动条，卡片固定宽度
- 当前时段放在首位
- 无数据时整个区块隐藏（`return null`）
- 每张卡：时间 / 图标 / 温度 / 风向风力

**验收**：手机宽度下可顺滑横向滚动，不带动整页横向滚动

**⛔ 本步不做**：不加折线图

---

## T2.4 预警横幅
**文件**：`AlarmBanner.tsx`

**要求**
- 无预警时 `return null`，**不占任何空间**
- 多个预警横向切换
- 展示类型 + 级别 + 内容，级别用颜色区分

**验收**
1. 无预警城市：页面无留白
2. 有预警城市：横幅正常展示

**⛔ 本步不做**：不做预警通知推送

---

# S3 · 收藏城市

## T3.1 storage 封装
**文件**：`src/utils/storage.ts`

**要求**
- `readJSON<T>(key): T | null` / `writeJSON(key, value): boolean`
- 全部 try/catch，捕获配额满（`QuotaExceededError`）与隐私模式禁用（`SecurityError`）
- 解析失败时清掉脏数据并返回 `null`
- 写入结构带 `version` 字段，便于以后迁移

**验收**：浏览器隐私模式下打开页面不白屏，控制台无未捕获异常

**⛔ 本步不做**：不写业务 key 名（留给 T3.2）

---

## T3.2 useFavorites
**文件**：`src/hooks/useFavorites.ts`、`src/hooks/useLocalStorage.ts`

**要求**
- key：`weather-app:favorites`
- 按 `cityId` 去重
- 上限 10 个，超出返回失败标识由 UI 提示
- 暴露 `favorites / isFavorite / addFavorite / removeFavorite / clearFavorites`
- 写入失败时降级为内存态并返回标记，不抛异常

**验收**：单测覆盖去重、上限、LocalStorage 抛异常时的降级

**⛔ 本步不做**：不做拖拽排序

---

## T3.3 收藏列表与确认弹窗
**文件**：`FavoriteCities.tsx`、`FavoriteButton.tsx`、`ConfirmDialog.tsx`

**要求**
- 收藏按钮状态明确（已收藏 / 未收藏），点击有过渡
- 收藏列表点击即切换城市
- 删除前二次确认
- 重复收藏提示「该城市已在收藏中」；超上限提示「最多收藏 10 个城市」
- 无收藏时显示引导文案

**验收**：收藏 3 个 → 刷新 → 仍在 → 点第 2 个能切换 → 删除需确认

**⛔ 本步不做**：不做排序、不做温度自动刷新

---

# S4 · 状态与收尾

## T4.1 加载 / 错误 / 空状态
**文件**：`Loading.tsx`、`ErrorState.tsx`、`EmptyState.tsx`、`ProgressBar.tsx`

**要求**
- 首次加载：骨架屏，不出现空白闪烁
- 切换城市：保留旧数据 + 顶部细进度条（不整页 loading）
- 错误态：`role="alert"`，文案友好，重试按钮可用
- 空状态：引导文案 + 搜索入口

**验收**：DevTools 限速 Slow 3G 下观察三种状态切换自然

**⛔ 本步不做**：不加 toast 库

---

## T4.2 Home 组装与响应式
**文件**：`src/pages/Home.tsx`、`SearchBar.tsx`

**要求**
- 按 PRD 布局顺序组装
- 搜索框回车可提交；空输入时按钮禁用
- 6 个热门城市标签，点击切换
- 桌面端居中 `max-width: 720px`
- 搜索框带 `label` / `aria-label`；装饰图标 `aria-hidden`

**验收**：375px / 768px / 1280px 三个宽度下布局均正常

**⛔ 本步不做**：不加路由、不加新页面

---

## T4.3 缓存与重复请求治理
**文件**：`src/hooks/useWeather.ts`、`src/constants/weather.ts`

**要求**
- 同城市 10 分钟内命中内存缓存，不重复请求
- 缓存命中时不要闪 loading
- `forceRefresh` 参数只在用户主动点重试时使用
- 缓存只存内存，不落 LocalStorage

**验收**：连续点击同一个城市 3 次，Network 面板只有 1 次请求

**⛔ 本步不做**：不做持久化缓存

---

## T4.4 全量回归与构建
**目标**：收口，交付可用 MVP。

**要求**：逐条跑 PRD 的 11 条验收标准

**必须执行的命令**
```bash
npm run typecheck     # 0 错误
npm run test          # 全绿
npm run build         # 成功
npm run lint          # 无 error
```

**额外自查**
- 浏览器控制台无 error
- `git status` 里**没有** `.env.local`
- 全局搜索 `appsecret`，源码中只应出现在读取环境变量的那一行
- 检查 `dist/` 产物：若走代理，产物 JS 中不应出现真实 appsecret

**⛔ 本步不做**：不加新功能，只修回归发现的问题

---

# S5 · 可选：7 天预报

## T5.1 接入第二个接口
**前置条件**：S0 ~ S4 全部验收通过，MVP 已可用。

**为什么单独做**：`v63` 不返回多日数据，7 天预报必须接第二个接口，响应结构完全不同，是最大的复杂度来源。

**要求**
1. 先在控制台确认七日接口的线路与 `version`，**抓一次真实响应存成 fixture**
2. 新增环境变量 `WEATHER_FORECAST_API_BASE` + 独立的 Vite 代理规则
3. 新增 `normalizeDailyForecast()`，按 fixture 写（字段在 `data[]` 数组内）
4. 与实况接口 `Promise.all` 并行请求，**七日失败不影响实况展示**
5. 补 `DailyForecast.tsx` 展示与对应单测

**验收**
1. 7 天列表正常展示，日期 / 星期 / 高低温正确
2. 断掉七日接口（临时改错线路），实况部分仍然正常显示

**⛔ 本步不做**：不做温度趋势图

---

# 附：回归检查清单（每次改动后抽查）

| # | 检查项 | 通过 |
|---|--------|------|
| 1 | 搜索中文城市名返回正确城市 | ☐ |
| 2 | 天气图标与文字一致（晴就显示晴天图标） | ☐ |
| 3 | 温度无 `NaN` / `undefined` / `0°` 误显示 | ☐ |
| 4 | AQI 卡片颜色随等级变化 | ☐ |
| 5 | 24 小时可横向滚动 | ☐ |
| 6 | 无预警不占位，有预警正常显示 | ☐ |
| 7 | 收藏刷新后仍在，可切换、可删除 | ☐ |
| 8 | 快速切换 3 个城市，结果与最后一次点击一致 | ☐ |
| 9 | Offline 下报错，恢复网络后重试成功 | ☐ |
| 10 | `typecheck` / `test` / `build` 均通过 | ☐ |
| 11 | `git status` 无 `.env.local` | ☐ |
