# 天气查询应用技术设计（MVP 精简版）

> 本文档是**冻结契约**。类型与字段映射定下后不要随手改，组件按契约写就不会互相打架。

## 技术栈
React 18 + TypeScript + Vite 5 + Tailwind CSS v4（`@tailwindcss/vite`）+ Vitest
代码规范：ESLint + Prettier（无分号 / 单引号 / printWidth 100）

## 环境变量（已配置，勿改）
```bash
# .env.local（不提交）
VITE_WEATHER_API_BASE=/
VITE_WEATHER_APPID=你的appid
VITE_WEATHER_APPSECRET=你的appsecret

# 仅 Vite 开发服务器读取，不进前端产物
WEATHER_API_BASE=http://pddfps.tianqiapi.com/api
WEATHER_APPID=你的appid
WEATHER_APPSECRET=你的appsecret
```

密钥安全三条铁律：
1. `.env.local` 必须在 `.gitignore` 中，**永不提交**
2. 代码里只允许出现 `import.meta.env.VITE_WEATHER_APPID`，不允许出现明文 Key
3. 日志里不打印完整请求 URL（含 appid）

> 说明：开发环境走 Vite 代理，密钥由 `vite.config.ts` 在服务端注入，前端 URL 中的密钥会被代理覆盖。**正式发布需改为服务端代理**，否则 `VITE_` 变量会进入前端产物。

---

## 目录结构
```
src/
  components/     SearchBar CurrentWeather WeatherMetrics AirQualityCard
                  HourlyForecast AlarmBanner WeatherIcon FavoriteButton
                  FavoriteCities ConfirmDialog Loading ErrorState EmptyState
  pages/          Home.tsx
  services/       weather.ts          # 唯一 HTTP 入口
  hooks/          useWeather.ts       # 拉取 + 缓存 + 状态
                  useFavorites.ts     # 收藏城市
                  useLocalStorage.ts  # storage 通用封装
  utils/          weather.ts format.ts storage.ts
  constants/      weather.ts
  types/          weather.ts
```

---

## 数据契约（冻结）

### 归一化后的类型（组件只认这些）
```ts
export type WeatherIconKey =
  | 'qing' | 'yun' | 'yin' | 'yu' | 'lei' | 'xue' | 'wu' | 'shachen' | 'bingbao'

export type AqiCategory = 'excellent' | 'good' | 'light' | 'moderate' | 'heavy' | 'severe'

export interface AirQuality {
  aqi: number
  pm25: number
  level: string
  tips: string
  category: AqiCategory
}

export interface CurrentWeather {
  cityId: string
  city: string
  country: string
  date: string          // '2026-09-17'
  week: string          // '星期四'
  updatedAt: string     // '16:09'
  weather: string       // '晴'
  weatherIcon: WeatherIconKey
  temperature: number | null   // null = 数据缺失，UI 显示 '--'
  high: number | null
  low: number | null
  windDirection: string
  windScale: string
  windSpeed: string
  humidity: string
  visibility: string
  pressure: string
  airQuality: AirQuality
}

export interface HourlyForecast {
  time: string          // '20时'
  weather: string
  weatherIcon: WeatherIconKey
  temperature: number | null
  windDirection: string
  windScale: string
}

export interface WeatherAlarm {
  type: string
  level: string
  content: string
}

export interface FavoriteCity {
  cityId: string
  city: string
  addedAt: string              // ISO 字符串
  lastTemperature?: number | null
}

export interface WeatherBundle {
  current: CurrentWeather
  hourly: HourlyForecast[]
  alarms: WeatherAlarm[]
}
```

### API 原始响应（v63，字段全是字符串）
```ts
export interface RawAlarm {
  alarm_type: string
  alarm_level: string
  alarm_content: string
}

export interface RawHourly {
  hours: string
  wea: string
  wea_img: string
  tem: string
  win: string
  win_speed: string
}

export interface WeatherApiRaw {
  cityid: string
  date: string
  week: string
  update_time: string
  city: string
  country: string
  wea: string
  wea_img: string
  tem: string
  tem1: string
  tem2: string
  win: string
  win_speed: string
  win_meter: string
  humidity: string
  visibility: string
  pressure: string
  air: string
  air_pm25: string
  air_level: string
  air_tips: string
  hours?: RawHourly[]
  alarm?: RawAlarm[] | RawAlarm      // 可能是对象也可能是数组！
  errcode?: number
  errmsg?: string
}
```

### Hook 返回值
```ts
interface UseWeatherResult {
  current: CurrentWeather | null
  hourly: HourlyForecast[]
  alarms: WeatherAlarm[]
  loading: boolean
  error: string | null
  /** 切换城市 */
  loadWeather: (city?: string, cityId?: string) => void
  /** 强制重试：无参数，用当前城市重新请求；无当前城市时用默认城市 */
  refresh: () => void
}
```

---

## API 契约

### 实况接口
- 请求方式：`GET`
- 前端请求路径：`/api?version=v63&...`（开发环境由 Vite 代理转发并注入密钥）
- 后端线路：`http://pddfps.tianqiapi.com/api`

| 参数 | 必填 | 说明 |
|------|------|------|
| `version` | 是 | 固定 `v63` |
| `appid` / `appsecret` | 是 | 由代理注入 |
| `city` | 否 | 城市名，**不带"市 / 区 / 县"** |
| `cityid` | 否 | 城市 ID，优先级高于 `city` |
| `unescape` | 否 | 传 `1` 让返回中文而不是 Unicode 转义 |
| `vue` | 否 | 传 `1` |

`city` / `cityid` 都不传时返回当前 IP 城市。

### 字段映射表
| API 字段 | 含义 | 映射到 |
|----------|------|--------|
| `cityid` | 城市 ID | `current.cityId` |
| `city` / `country` | 城市 / 国家 | `current.city` / `country` |
| `date` / `week` | 日期 / 星期 | `current.date` / `week` |
| `update_time` | 更新时间 | `current.updatedAt` |
| `wea` | 天气文字 | `current.weather` |
| `wea_img` | 天气图标标识 | `current.weatherIcon`（见下方映射表） |
| `tem` / `tem1` / `tem2` | 实况温度 / 最高 / 最低 | `temperature` / `high` / `low` |
| `win` / `win_speed` / `win_meter` | 风向 / 风力 / 风速 | `windDirection` / `windScale` / `windSpeed` |
| `humidity` / `visibility` / `pressure` | 湿度 / 能见度 / 气压 | 同名 |
| `air` / `air_pm25` / `air_level` / `air_tips` | AQI / PM2.5 / 等级 / 提示 | `airQuality.*` |
| `hours[]` | 24 小时逐时 | `hourly[]` |
| `alarm` | 气象预警（对象或数组） | `alarms[]` |
| `errcode` / `errmsg` | 错误码 / 错误信息 | 抛出为 `error` |

### 错误码
| 错误码 | 提示文案 |
|--------|----------|
| 100 | API 配置错误，请检查 appid |
| 101 | API 密钥错误，请检查 appsecret |
| 102 | 请求次数已达上限，请稍后再试 |
| 103 | 当前账号无该城市查询权限 |
| 其他 | 展示 `errmsg`，提供重试 |

---

## 归一化规则（bug 重灾区，逐条照做）

1. **`wea_img` 是拼音字符串**，不是数字。
   `qing→晴`、`yun→云`、`yin→阴`、`yu→雨`、`lei→雷`、`xue→雪`、`wu→雾`、`shachen→沙尘`、`bingbao→冰雹`
   未匹配到时兜底 `'yun'`。
   ⚠️ 不要写成 `{'0':'qing', '1':'yun', ...}` —— 线上永远不会命中，会导致所有天气都显示成"云"。

2. **city 参数不要手动 `encodeURIComponent`**。
   `URLSearchParams` 在 `toString()` 时已自动编码，手动再编码会变成双重编码
   （`青岛` → `%25E9%259D%2592...`），接口收到的是乱码，搜索必然失败。
   ✅ 正确写法：`params.set('city', city)`

3. **数值字段全部是字符串**，且可能为 `''` 或非数字。
   统一用 `toNumber(v: string): number | null`：
   `const n = Number(v); return Number.isFinite(n) ? Math.round(n) : null`
   ⚠️ 不要用 `Number(v) || 0` —— 会把缺失值伪装成 0°，页面显示"0°"是错的，应该显示 `--`。

4. **`alarm` 可能是对象也可能是数组**：`const list = Array.isArray(raw.alarm) ? raw.alarm : raw.alarm ? [raw.alarm] : []`

5. **`hours` 可能缺失**：`raw.hours ?? []`

6. **`humidity` 自带 `%`**：不要在 UI 里再拼一次 `%`。归一化时统一去掉，UI 层拼。

7. **所有归一化函数必须是纯函数**，只吃 `raw` 返回新对象，不读全局、不发请求。

---

## 避坑清单（现有代码已踩过的坑）

| # | 坑 | 正确做法 |
|---|----|---------|
| 1 | `city` 被二次编码 | `params.set('city', city)` |
| 2 | 图标映射用了数字 key，全部落到兜底值 | 用拼音字符串 key |
| 3 | `AbortController` 建了但没传给 `fetch` —— 取消无效 | 控制器由 `useWeather` 持有并传入 `fetchWeather(url, signal)`；或改用请求序号（requestId）丢弃过期响应 |
| 4 | `refresh()` 依赖缓存存在，请求失败后重试按钮点了没反应 | `refresh()` 必须记录「上一次请求的城市」，无缓存时用该城市重试；无任何记录时退回默认城市 |
| 5 | 快速切换城市时旧响应覆盖新响应 | 每次请求带一个自增 `requestId`，响应回来时比对，不一致就丢弃 |
| 6 | Tailwind 动态类名被 purge | `constants/weather.ts` 里写完整类名字符串 |

---

## 边界情况

| 场景 | 处理 |
|------|------|
| 城市名为空 | 禁用搜索按钮 |
| 城市不存在 | 提示「未找到该城市」，保留原数据 |
| 网络断开 / 超时（8s） | 错误态 + 可用的重试按钮 |
| 接口次数超限（102） | 友好提示，不自动重试 |
| `hours` / `alarm` 缺失 | 按空数组处理，对应区块隐藏 |
| 温度字段非法 | 显示 `--`，不显示 `NaN` |
| 收藏重复 / 超上限 | 去重（按 `cityId`）；上限 10 个，超出提示 |
| LocalStorage 被禁用 / 配额满 | try/catch，降级为内存态并提示 |
| 缓存 | 同城市 10 分钟内命中内存缓存，不重复请求 |

---

## 阶段三（可选）：7 天预报

**为什么单独拆出来**：`v63` 是实况接口，**不返回多日数据**。7 天预报需要接第二个接口，是 MVP 里最大的复杂度来源，所以放到最后做。

- 接口：七日天气接口，线路与 version 以你的账号控制台为准（免费版通常为 `version=v1`，线路 `https://www.tianqiapi.com/api`）
- 响应结构与 v63 **完全不同**——多日数据在 `data[]` 数组里：

```ts
export interface RawDaily {
  day: string        // '17日(今天)'
  date: string
  week: string
  wea: string
  wea_img: string
  tem1: string       // 高温
  tem2: string       // 低温
  win: string
  win_speed: string
  humidity: string
  air: string
  air_level: string
  air_tips: string
}

export interface ForecastApiRaw {
  cityid: string
  update_time: string
  city: string
  country: string
  data: RawDaily[]
  errcode?: number
  errmsg?: string
}
```

```ts
export interface DailyForecast {
  date: string
  week: string
  weather: string
  weatherIcon: WeatherIconKey
  high: number | null
  low: number | null
  windDirection: string
  windScale: string
}
```

实现要点：
1. 给第二个接口单独加一条 Vite 代理规则（`/api/forecast`）和独立的环境变量 `WEATHER_FORECAST_API_BASE`
2. **先跑通请求、把真实响应存成 fixture，再写归一化**——不要照着文档猜字段
3. 两个接口并行请求（`Promise.all`），任一失败不影响实况展示
