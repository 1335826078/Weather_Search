# 天气查询应用 PRD（MVP 精简版）

## 一句话定位
输入城市名，看到实时天气、未来 24 小时趋势和气象预警；把常看的城市收藏起来，下次一键切换。

## 目标用户
个人自用 / 练手项目。移动端优先，不注册、不登录、数据只存本地。

---

## MVP 范围（本次只做这些）

### 1. 城市搜索
- 输入城市名（中文，**不带"市 / 区 / 县"**），回车或点按钮触发
- 6 个热门城市快捷标签，点击即切换：北京、上海、广州、深圳、杭州、成都
- 首次打开默认展示北京
- 搜不到时提示「未找到该城市」，**保留原有数据不清空**

### 2. 实况天气
城市名 / 更新时间 / 天气图标 + 文字 / **实时温度（大字）** / 今日最高最低温 / 湿度 / 能见度 / 气压 / 风向 / 风力 / 风速

### 3. 空气质量
AQI 数值 + 等级 + 提示文案。按等级配色：优 = 绿、良 = 黄、轻度污染及以上 = 橙 / 红

### 4. 未来 24 小时
横向滚动小时卡片：时间 / 图标 / 温度 / 风向风力。无数据时**整个区块隐藏**

### 5. 气象预警
顶部横幅：类型 + 级别 + 内容。**无预警时不占任何空间**。多个预警可横向切换

### 6. 收藏城市
- 当前城市可收藏 / 取消收藏，按钮状态明确区分
- 收藏列表点击即切换城市
- 刷新页面后仍然存在（LocalStorage）
- 删除需二次确认
- 上限 10 个，超出时提示；重复收藏提示「该城市已在收藏中」

### 7. 页面状态
- 加载：骨架屏；切换城市时**保留旧数据** + 顶部细进度条（避免高度跳动）
- 错误：友好文案 + **可用的重试按钮**
- 空：无收藏城市时显示引导文案

---

## MVP 明确不做
深色模式、温度趋势图表、多城市对比、生活指数、IP 定位、**7 天预报**（放阶段三，见技术设计文档）

---

## 界面布局
移动端优先，桌面端居中 `max-width: 720px`：

```
┌─────────────────────────┐
│ 搜索框 + 热门城市标签    │
├─────────────────────────┤
│ 预警横幅（有则显示）     │
├─────────────────────────┤
│ 当前天气卡               │
│  城市名 · 更新时间       │
│  图标  25°  晴          │
│  最高 28° / 最低 22°     │
│  [湿度][能见度][气压]    │
│  [风向风力][风速]        │
│  空气质量卡              │
├─────────────────────────┤
│ 24 小时（横向滚动）      │
├─────────────────────────┤
│ 收藏城市                 │
└─────────────────────────┘
```

---

## 验收标准（逐条可测）

| # | 验收项 |
|---|--------|
| 1 | 搜索"青岛"能返回青岛天气 |
| 2 | 6 个热门城市标签点击均能切换成功 |
| 3 | 实时温度大字显示；高低温、湿度等指标无 `NaN` / `undefined` |
| 4 | AQI 卡片颜色随等级变化 |
| 5 | 24 小时可横向滚动，当前时段在首位 |
| 6 | 无预警城市不显示横幅，页面无空白占位 |
| 7 | 收藏 3 个城市 → 刷新页面 → 仍在，点击可切换 |
| 8 | 收藏第 11 个时提示上限；重复收藏提示已存在 |
| 9 | 开发者工具切 Offline → 提交搜索 → 显示错误，且**重试按钮可用** |
| 10 | 快速连续切换 3 个城市，最终展示的城市与最后一次点击一致 |
| 11 | `npm run typecheck` 与 `npm run build` 均无错误 |
